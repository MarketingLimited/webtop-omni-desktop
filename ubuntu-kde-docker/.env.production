# Production Environment Configuration

# Usernames and Passwords (Change these in production!)
DEV_USERNAME=marketing
DEV_PASSWORD=SecureMarketing2024!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=SecureAdmin2024!
ROOT_PASSWORD=SecureRoot2024!
TTYD_USER=terminal
TTYD_PASSWORD=SecureTerminal2024!

# User and Group IDs
DEV_UID=1000
DEV_GID=1000

# Timezone
TZ=UTC

# SSL Configuration
SSL_CERT_PATH=/etc/ssl/certs/marketing.crt
SSL_KEY_PATH=/etc/ssl/private/marketing.key

# Domain Configuration
DOMAIN=marketing.yourdomain.com

# Database Configuration (if using external DB)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=marketing_agency
DB_USER=marketing_user
DB_PASSWORD=SecureDBPassword2024!

# Redis Configuration (if using external Redis)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=SecureRedisPassword2024!

# Security
DISABLE_ROOT_LOGIN=true
ENABLE_FIREWALL=true

# Performance
MAX_MEMORY=8G
MAX_CPU=4